name: Check tools list

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  check-tools-list:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
      - run: uv run python scripts/build_tools_list.py --check
